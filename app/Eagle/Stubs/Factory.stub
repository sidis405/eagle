public static function make($myeField)
    {
        $item = new static(compact('myeField'));

        return $item;
    }

    public static function edit($item_id, $myField)
    {
        $item = static::find($item_id);

        $item->myField = $myField;

        return $item;
    }